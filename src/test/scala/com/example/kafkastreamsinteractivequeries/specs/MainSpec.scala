package com.example.kafkastreamsinteractivequeries

import org.scalatest.junit.JUnitRunner

//import org.json4s._
//import org.json4s.JsonDSL._
//import org.json4s.jackson.JsonMethods._

class MainSpec extends UnitSpec {

  // before all tests have run
  override def beforeAll() = {
    super.beforeAll()
  }

  // before each test has run
  override def beforeEach() = {
    super.beforeEach()
  }

  // after each test has run
  override def afterEach() = {
    //myAfterEach()
    super.afterEach()
  }

  // after all tests have run
  override def afterAll() = {
    super.afterAll()
  }

  //////////////////////////////////////////////////////////////////////////////
  //////////////////////////////////////////////////////////////////////////////
  // Tests start
  //////////////////////////////////////////////////////////////////////////////
  //////////////////////////////////////////////////////////////////////////////

  describe("some thing (a class, function, or other unit of functionality)")  // ------------------------------------------------
  {
    it("should do this...") {
      // For hints on how to write 'should statements', see the matchers documentation at
      // http://www.scalatest.org/user_guide/using_matchers
      "some value" should be ("some value")
    }
  }
}

  
